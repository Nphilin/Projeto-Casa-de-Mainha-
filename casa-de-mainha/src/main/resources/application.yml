server:
  port: 8080

spring:
  application:
    name: Casa-De-Mainha
  datasource:
    url: jdbc:h2:mem:Casa-De-Mainha
    username: admin
    password: turma2nb
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true

springdoc:
  swagger-ui:
    path: /swagger-ui.html